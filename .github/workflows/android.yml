name: Android CI

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build FamilyNest debug APK
        run: ./gradlew :app:assembleDebug --no-daemon --stacktrace

      - name: Build Dispensa debug APK
        run: ./gradlew :dispensa:assembleDebug --no-daemon --stacktrace

      - name: Upload FamilyNest APK
        uses: actions/upload-artifact@v4
        with:
          name: FamilyNest-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

      - name: Upload Dispensa APK
        uses: actions/upload-artifact@v4
        with:
          name: Dispensa-debug-apk
          path: dispensa/build/outputs/apk/debug/dispensa-debug.apk
          if-no-files-found: error
