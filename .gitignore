*.iml
.gradle/
.kotlin/
/local.properties
.idea/
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties

# Keep the template google-services.json in the repo, but ignore real secrets.
# (Replace app/google-services.json with your real one locally — it is safe to
#  commit a Firebase config, but never commit signing keystores or service accounts.)
*.jks
*.keystore
keystore.properties
google-services-real.json
service-account*.json
